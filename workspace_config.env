# DJI EP Workspace Configuration File
# This file contains environment variable definitions for the DJI EP workspace
# Modify these paths according to your system setup

# =============================================================================
# User and Workspace Paths
# =============================================================================

# User home directory (usually automatically detected)
# USER_HOME="${HOME}"

# Base workspace directory where your projects are located
USER_WORKSPACE="${HOME}/git/me"

# DJI EP workspace directory
DJI_EP_WS="${USER_WORKSPACE}/Dji_EP_ws"

# =============================================================================
# Prometheus and PX4 Paths
# =============================================================================

# Prometheus installation path
PROMETHEUS_PATH="${HOME}/pixhawk4"

# PX4 installation path
PX4_PATH="${HOME}/pixhawk4"

# Prometheus MAVROS path
PROMETHEUS_MAVROS_PATH="${HOME}/prometheus_mavros"

# =============================================================================
# Advanced Configuration (Optional)
# =============================================================================

# ROS Distribution (default: noetic)
# ROS_DISTRO="noetic"

# Gazebo simulation speed factor (1.0 = real-time, >1.0 = faster, <1.0 = slower)
# PX4_SIM_SPEED_FACTOR="1.0"

# Enable verbose output during environment setup (true/false)
# VERBOSE_SETUP="false"

# =============================================================================
# Instructions
# =============================================================================
# 
# To use this configuration file:
# 1. Copy this file to workspace_config.env in your DJI EP workspace root
# 2. Modify the paths above to match your system
# 3. The setup_env.bash script will automatically source this file if it exists
# 
# Example paths for different setups:
# 
# For standard installation:
# USER_WORKSPACE="${HOME}/catkin_ws"
# PROMETHEUS_PATH="${HOME}/Prometheus"
# PX4_PATH="${HOME}/PX4-Autopilot"
# 
# For custom installation:
# USER_WORKSPACE="/opt/robotics/workspaces"
# PROMETHEUS_PATH="/opt/robotics/prometheus"
# PX4_PATH="/opt/robotics/px4"
#
